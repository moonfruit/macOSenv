#!/bin/bash
if [[ -z $DARK_ENABLED ]] && hash it2check 2>/dev/null && it2check; then
    if ! hash it2profile 2>/dev/null || [[ $(it2profile -g) != dark ]]; then
        /opt/homebrew/bin/hexyl "$@" | solarized.sh
        exit
    fi
fi
exec /opt/homebrew/bin/hexyl "$@"
