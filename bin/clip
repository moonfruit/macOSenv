#!/bin/bash

FILE=$1
if [[ $FILE == "" || ! -r $FILE ]]; then
	echo "$(basename "$0") <file>"
	exit 1
fi

exec osascript \
	-e 'on run args' \
	-e 'set the clipboard to POSIX file (first item of args)' \
	-e end \
	"$(realpath "$FILE")"
