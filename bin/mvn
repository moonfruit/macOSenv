#!/usr/bin/env bash
set -euo pipefail
source "$ENV/lib/bash/console.sh"

MVN="$(brew --prefix)/bin/mvn"

if is-solarized; then
    exec "$MVN" -Dstyle.color=always "$@" | solarized.sh
else
    exec "$MVN" "$@"
fi
