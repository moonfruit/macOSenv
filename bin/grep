#!/bin/bash
GREP=/usr/local/bin/ggrep
DEFAULT=-E
for ARG in "$@"; do
	if echo "$ARG" | "$GREP" -Gxq '\-[a-zA-Z]*[EFGP].*'; then
		DEFAULT=
		break
	fi
done
exec "$GREP" $DEFAULT --color=auto -dskip --exclude-dir={.bzr,CVS,.git,.hg,.svn} "$@"
