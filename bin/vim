#!/usr/bin/env bash
if [[ $LC_TERMINAL == iTerm2 ]]; then
	VIM=(proxy lvim)
else
	VIM=(nvim)
fi

NAME="${0##*/}"
if [[ $NAME == view ]]; then
	VIM+=(-R)
elif [[ $NAME == vimdiff ]]; then
	VIM+=(-d)
elif [[ $NAME == vimex ]]; then
	VIM+=(-e)
fi

exec "${VIM[@]}" "$@"
