{
  "dns": {
    "servers": [
      { "tag": "dns-resolver", "type": "udp", "server": "223.5.5.5" },
      { "tag": "dns-direct", "type": "h3", "server": "dns.alidns.com", "domain_resolver": "dns-resolver" },
      { "tag": "dns-proxy", "type": "tls", "server": "1.1.1.1" },
      { "tag": "dns-gingkoo", "type": "udp", "server": "10.1.2.59", "server_port": 1053 },
      { "tag": "dns-home", "type": "udp", "server": "192.168.50.1" },
      {
        "tag": "dns-hosts",
        "type": "hosts",
        "predefined": {
          "asusrouter.com": "192.168.50.1",
          "router.asus.com": "192.168.50.1",
          "www.asusrouter.com": "192.168.50.1"
        }
      }
    ],
    "rules": [
      { "ip_accept_any": true, "server": "dns-hosts" },
      { "domain_suffix": "server.gingkoo", "server": "dns-gingkoo", "rewrite_ttl": 2592000 },
      { "clash_mode": "Global", "server": "dns-proxy" },
      { "clash_mode": "Direct", "server": "dns-direct" }
    ],
    "final": "dns-direct",
    "independent_cache": true,
    "reverse_mapping": true,
    "strategy": "prefer_ipv4"
  },
  "inbounds": [
    {
      "type": "direct",
      "tag": "dns-in",
      "listen": "127.0.0.1",
      "listen_port": 53
    }
  ],
  "route": {
    "rules": [
      {
        "action": "sniff"
      },
      {
        "protocol": "dns",
        "action": "hijack-dns"
      }
    ],
    "default_domain_resolver": "dns-direct"
  }
}
